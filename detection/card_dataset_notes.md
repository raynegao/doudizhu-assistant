# Card Dataset Notes

- Use 1280x720 screenshots captured from the game client.
- Label each visible card as a single bounding box using YOLO format.
- Index classes consistently with `cards.py` so downstream parsing stays aligned.
